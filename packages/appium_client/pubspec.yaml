name: appium_client
description: Minimal Dart Appium/WebDriver client for iOS Assist Studio.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.9.2

dependencies:

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
