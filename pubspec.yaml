name: ios_assist_studio_workspace
description: iOS Assist Studio V2.0 Flutter/Dart monorepo workspace.
publish_to: none

environment:
  sdk: ^3.9.2

dependencies:
  studio_runtime:
    path: packages/studio_runtime

dev_dependencies:
  melos: ^6.3.2
