name: workflow_dsl
description: Project DSL models and validation for iOS Assist Studio workflows.
version: 1.0.0

environment:
  sdk: ^3.9.2

dependencies:

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
