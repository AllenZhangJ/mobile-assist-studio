# iOS Assist Studio V4.0 Development Roadmap

## 0. TL;DR

- V4.0 用终局倒推，工程正推。
- 终局是本地视觉移动自动化工作站，融合 Airtest、Pyxelator、Appium Inspector 和 appium-mcp。
- 第一版必须 iOS 真机深验证，Android 真机完成最小冒烟。
- 每批都必须保持 Node 不回流、Runtime 不旁路、Project DSL 不分裂、证据不泄露。

## 1. Backward Target

V4.0 终局能力：

```text
连接设备
  -> 检查界面
  -> 录制动作
  -> 创建目标
  -> 编排流程
  -> 视觉定位
  -> 串行执行
  -> 失败暂停
  -> 本地报告
  -> AI 建议修复
```

## 2. Batch 0: Truth Sources And Legal Groundwork

### Goal

建立 4.0 真源、第三方吸纳规则和不可逆架构决策。

### Deliverables

- `docs/V4.0-PRD-Mobile-Automation-Workstation.md`
- `docs/V4.0-Architecture-Integrated-Mobile-Workstation.md`
- `docs/V4.0-Open-Source-Integration-Plan.md`
- `docs/V4.0-Development-Roadmap.md`
- `docs/decisions/ADR-002-v4-open-source-fusion-and-node-exit.md`
- `THIRD_PARTY_NOTICES.md`
- `docs/README.md` 路由更新
- `AI_PROJECT_CONTEXT.md` 真源更新
- `AGENTS.md` 协作规则更新

### Acceptance

- 后续 AI 进入项目能识别 V4.0 任务入口。
- 文档明确 Node 不恢复。
- 文档明确 Android 首版真机冒烟。
- 文档明确四个核心开源项目的吸纳方式。
- 文档明确复制源码治理规则。

### Current Implementation Slice

- V4.0 真源文档、ADR、第三方通知和协作入口已落地。
- 当前 Flutter / Dart monorepo 包默认使用 `publish_to: none`，避免内部 Runtime、DSL、Appium Client 和 Design System 被误发布为独立公共包。
- 包级许可证不得保留模板 TODO；当前 Design System 包已明确为项目内部包，除非仓库级许可证或书面授权另行授予权限。
- `THIRD_PARTY_NOTICES.md` 仍只登记复制或 vendored 第三方源码；普通包依赖继续由 package manager 文件和包级 README 表达。

## 3. Batch 1: Runtime Foundation And Node Exit

### Goal

把 V4.0 所需 Runtime 边界先立稳，防止后续能力变成散落实现。

### Deliverables

- Runtime 平台中立接口整理。
- iOS Adapter 包装现有能力。
- Android Adapter MVP 骨架。
- Python Sidecar capability probe。
- VisionProvider contract。
- TargetResolver contract。
- Inspector Core model。
- AI Tool Registry model。
- Node usage boundary check 升级为 V4 规则。
- Legacy Node 删除计划。

### Acceptance

- Flutter / Dart 主路径不调用 Node。
- iOS 既有连接和执行不退化。
- fake iOS / fake Android driver 测试通过。
- Python Sidecar 缺失时主流程可用。
- Runtime snapshot 能表达平台、能力和资源锁。

## 4. Batch 2: iOS Deep Validation And Android Smoke

### Goal

让 V4.0 从一开始就是双平台闭环，不让 Android 成为文档里的占位符。

### Deliverables

iOS：

- iOS 真机连接。
- 截图。
- Tap / Swipe / Input。
- 基础 workflow 执行。
- 本地证据。

Android：

- ADB 当前设备发现。
- Appium UiAutomator2 session。
- Android 截图。
- Tap / Swipe / Input 冒烟。
- 基础 workflow 冒烟。
- logcat 摘要。
- Android 短诊断。

### Acceptance

- iOS 真机可完成完整基础流程。
- Android 真机至少完成连接、截图、点击、滑动、输入和一次基础 workflow。
- 两个平台失败时都能生成脱敏诊断。
- 两个平台都能写入本地 evidence。

### Current Implementation Slice

- Runtime 已具备 Android ADB 设备发现、未授权 / 离线 / 多设备阻断、serial 脱敏和 logcat 摘要清理。
- Device 本机环境检查已接入 Android ADB 准备项：可展示无设备、未授权、离线、多设备或 ADB 不可用的短提醒和下一步，但只作为 V4 Android 准备度提示，不阻断当前 iOS 一键连接。
- Runtime 已具备 Android UiAutomator2 adapter 的 fake Appium session、截图、Tap、Swipe、Input、日志和断开测试。
- 项目已固定 Appium `xcuitest` 与 `uiautomator2` 两个平台 driver 依赖；full smoke 自动准备会先检查所需 driver 是否已被 Appium 识别，缺失时在“驱动组件”项中阻断。
- Runtime 已具备跨平台 `MobileDriverSmokeRunner`，可把 driver 能力摘要、截图、可选动作、日志和失败兜底写入现有本地 evidence。
- 本地已提供 `npm run v4:ios-smoke` 作为 iOS 真机冒烟入口；默认只建会话、截图和留档，显式加 `-- --allow-actions` 才执行真实 Tap / Swipe / Input，加 `-- --workflow-basic --allow-actions` 可执行基础 Project DSL workflow。
- 本地已提供 `npm run v4:android-smoke` 作为 Android 真机冒烟入口；默认只建会话、截图和留档，显式加 `-- --allow-actions` 才执行真实 Tap / Swipe / Input，加 `-- --workflow-basic --allow-actions` 可执行基础 Project DSL workflow。当前若驱动不可达、无 Android 手机、未授权、离线或多设备阻断，会生成 `ANDROID_SMOKE_PREFLIGHT` 脱敏 Markdown / JSON 诊断；该诊断会被 readiness 索引，但不计作真实 Android smoke run。
- 本地已提供 `npm run v4:smoke:full` 作为双平台完整冒烟编排入口，会先用 Dart Runtime 能力自动准备本机 Appium 驱动、iOS 必要隧道和 Android ADB 可见性，再顺序执行 iOS 与 Android 的真实 Tap / Swipe / Input + 基础 Project DSL workflow，并在任一平台失败后仍继续生成 full smoke Markdown / JSON、readiness / completion audit 留档；当 Android 纳入 full smoke 且前置阻断时，会同步写入同 schema 的 `ANDROID_SMOKE_PREFLIGHT` 脱敏诊断，确保 readiness / acceptance 索引到最新 Android 阻断。`npm run v4:ios-smoke:full` 和 `npm run v4:android-smoke:full` 也复用同一个 full smoke 编排器，只跳过另一个平台，单平台排障时同样具备自动准备能力。iOS 18+ 隧道仍遵守系统授权边界：可先通过 Mac App 连接设备，或用 `npm run v4:ios-smoke:full:password-prompt` / `npm run v4:smoke:full:password-prompt` 从终端隐藏输入 Mac 密码启动本次 smoke 托管隧道；`password-stdin` 入口仅作为自动化备用；Android 准备只启动 ADB server 并检查唯一已授权手机，不执行设备动作。
- Runtime 已具备完整 Project DSL workflow 冒烟 evidence 闭环；Mac App Monitor 已接入 V4 验收卡，提供当前本地线索、最新 final acceptance 脱敏摘要、结构化终验门禁、现场补验清单和 full smoke / Android smoke / acceptance audit 命令复制入口；尚未完成 Android 真机现场冒烟留档。
- readiness 与归档终验会把截图证据绑定到最近一次平台 smoke run：iOS / Android 平台 run 必须同时具备 `smokeScreenshot` 事件和同 run 目录下的截图文件，只有全局旧截图或 UI 截图不能替代当前平台真机冒烟留证。
- readiness 的平台 smoke 记录数只统计 `run-*` 目录，普通诊断目录、前置诊断文件或 archive 输出不得计作真实平台 run。
- readiness 与归档终验对 full smoke 的完整判定必须同时要求 `completion.complete` 为真，以及 `iOS smoke`、`Android smoke` 两个步骤都为“通过”；单平台 full smoke 或缺步骤报告不得计作双平台 full smoke 完整通过。
- readiness、archive 和 final acceptance 读取最新 JSON 报告时必须按时间倒序选择最近一份结构有效的 JSON；写入中断或损坏的较新报告不得遮挡较旧有效留档。
- readiness 与 final acceptance 会把 iOS CoreDevice 视角和 usbmux USB 视角分开展示；当 CoreDevice 仅有历史不可用记录但 usbmux 未发现 USB iPhone 时，终验门禁、现场清单和下一步必须优先提示插线、解锁和信任，避免误判为 WDA 或 Appium 问题。
- final acceptance 的 Android 下一步必须读取 ADB 本机状态：无设备、未授权、离线和可用状态要给出不同提示，避免用户在未授权或离线时反复重跑 smoke。
- Android smoke preflight 自身也必须写入 ADB 状态计数和状态驱动下一步；无设备、未授权、离线、多设备不得退回泛化“连接手机”提示。
- final acceptance 选择 iOS / full smoke 命令时必须同时参考最近 full smoke 阻断和 readiness 当前 iOS 隧道状态；即使最新留档来自 Android-only smoke，只要本机 iOS 隧道不可达或数量为 0，也必须给出 `password-prompt` 命令。
- readiness 的 `nextSteps` 也必须输出命令级、状态驱动提示；iOS 默认给出密码版 smoke 命令，Android 必须根据无设备、未授权、离线、多设备和唯一可用状态给出不同下一步。

## 5. Batch 3: Inspector Core Productization

### Goal

吸收 Appium Inspector 的成熟价值，让用户看清当前 App。

### Deliverables

- Inspector 页面。
- Session 摘要。
- Capability 面板。
- 当前截图。
- 元素树。
- 元素属性。
- Source 查看。
- 命令面板。
- 截图高亮。
- iOS / Android 平台能力差异提示。

### Acceptance

- 用户不用懂 Appium 字段也能判断当前手机是否可检查。
- 高级字段进入 Advanced 面板。
- Inspector 错误不影响 Device / Execute 基础入口。
- Inspector 数据脱敏。

### Current Implementation Slice

- Runtime 已具备 `inspectCurrentScreen` 当前界面检查命令，串行读取截图和 Appium source，使用独立 busy 锁避免重复检查竞态。
- Runtime 已具备 Appium source 脱敏解析器，可生成元素树摘要、元素属性、bounds、元素数量和脱敏 Source 预览；原始 XML 不进入主界面状态。
- `StudioRuntimeSnapshot` 已持有最近一次 `InspectorSnapshot`；断开设备时会清空 Inspector 快照，避免展示旧界面结构。
- Device 页已接入“检查”面板，展示会话摘要、能力标签、元素树预览和脱敏 Source 片段；当前作为 Batch 3 Inspector MVP 入口，暂不新增一级导航。
- 已覆盖 Runtime Inspector 单元测试和 Device Inspector widget 测试；Inspector 失败只写事件，不污染 Device / Execute 主连接状态。

## 6. Batch 4: Target Library And Recorder Upgrade

### Goal

把录制从坐标动作升级为可复用目标资产。

### Deliverables

- Target Store。
- Target validator。
- Coordinate Target。
- Selector Target。
- Image Target metadata。
- Region Target。
- Text Target placeholder。
- 从截图创建目标。
- 从录制动作创建目标。
- Recorder 动作绑定 target。
- Workflow 节点引用 targetRef。
- 缺失 target 诊断。

### Acceptance

- 旧坐标流程仍可运行。
- 新流程可通过 targetRef 表达目标。
- 删除 target 会保护被引用的 workflow。
- 运行前检查能发现缺失目标。

### Current Implementation Slice

- Runtime 已具备本地 Target Library 快照、validator 和 JSON store；无效目标文件会降级为空目标库，不污染 Runtime snapshot。
- Runtime 已支持 Coordinate / Selector / Image / Region / Text 五类目标定义，其中坐标目标、区域目标、selector 目标和 text 目标可被 Tap 节点通过 `targetRef` 解析并串行点击。
- Project DSL 已允许 Tap 节点使用 `targetRef` 代替裸坐标；旧坐标 Tap 仍保持兼容。
- Runtime 项目级 workflow 校验已合并 targetRef 缺失诊断；保存 workflow、运行前检查和删除目标保护使用同一套引用规则。
- Recorder 生成流程时会把点击动作沉淀为坐标目标，并让 Tap 节点引用目标；时间线仍默认隐藏坐标，输入明文仍不进入日志摘要。
- Device 页已提供目标库摘要面板，可把当前截图中心保存为坐标目标；该动作只写 Runtime Target Library，不点击设备、不启动驱动。
- 已覆盖 Target Library store、引用保护、缺失 target、坐标 target 执行、区域 target 执行、selector target 执行、text target 执行、DSL targetRef、Recorder promote 和 Device 截图建目标 widget 回归测试。
- 尚未完成独立 Target Library 一级模块和完整视觉证据链；这些进入 Batch 6 / Batch 7。

## 7. Batch 5: Vision Core With Pyxelator And Airtest

### Goal

让视觉能力从概念变成可运行的目标解析层。

### Deliverables

- PyxelatorProvider。
- AirtestProvider。
- Image Template Resolver。
- Visual Assert。
- Visual Branch。
- Wait For Target。
- Tap Target。
- 置信度阈值。
- 低置信暂停。
- 视觉证据链。
- 模板测试工具。
- fixture screenshot 测试。

### Acceptance

- Pyxelator 可在 fixture 中定位模板。
- Airtest 可在 fixture 中完成视觉断言。
- VisionProvider 不直接点击设备。
- 低置信不会盲点。
- Run Detail 能展示视觉判断证据。

### Current Implementation Slice

- Runtime 已具备 `CompositeTargetResolver`，统一调度 VisionProvider，provider 只解析目标，不执行设备动作。
- Runtime 已具备 `CoordinateTargetProvider`，用于把坐标目标纳入 TargetResolver 统一语义。
- Runtime 已具备 `RegionTargetProvider`，可把区域目标解析为中心点和区域证据，provider 只返回结果，不执行点击。
- Runtime 已具备 `SelectorTargetProvider`，可在 Appium Source 可用时通过 `label=`、`value=`、`text=`、`type=` 或裸文本的受控短语法解析元素中心点；它不支持 XPath、CSS、脚本或任意表达式。
- Runtime 已具备 `TextTargetProvider`，可在 Appium Source 可用时通过 label / value 精确匹配文本并解析元素中心点；显式开启 Python 视觉增强后，`PythonOcrTextProvider` 可作为后续 OCR 解析层。
- Runtime 已具备 `PyxelatorFixtureVisionProvider`，可在小尺寸 PNG fixture 中完成图片目标模板匹配，输出中心点、区域、置信度和本地证据引用；超出本地操作量上限时会返回 unsupported，避免真实大截图卡住 Dart Runtime。
- Runtime 已具备 `AirtestFixtureVisionProvider`，以 fixture 方式表达视觉断言结果；它不运行 `.air` 文件、不启动 Python、不控制设备。
- Runtime 已具备短生命周期 `PythonVisionSidecarClient`、`PythonSidecarVisionProvider` 和 `PythonOcrTextProvider`，通过 stdin/stdout JSON 调用本地 Python 视觉脚本；provider 只返回坐标、区域、置信度和证据，不拥有点击能力。Python sidecar 已支持 `pyxelator`、`airtest` 和 `builtin` 三类图片后端：Pyxelator 后端会尝试常见模板定位 API，Airtest 后端会尝试 `airtest.aircv.find_template`，不可用时返回结构化 `unsupported` 并继续下一 provider，最终可落到内置 PNG 匹配兜底。OCR 文本后端优先尝试本机 `pytesseract`，缺少包、缺少系统 OCR 能力或 API 不匹配时只返回结构化不可用。Python 视觉由本机设置里的“视觉增强”显式开启，默认关闭，避免缺失环境时拖慢主流程。
- Visual Branch 已支持 `targetRef`：配置图片、selector 或 text 目标时会通过 TargetResolver 判断目标是否匹配；低置信、未找到或不支持会进入暂停态，不会继续后续动作。
- Wait For Target 已作为 Project DSL 节点进入 Runtime：节点会串行截图、通过 TargetResolver 解析目标，匹配后继续，超时、低置信、不支持或基础设施错误都会暂停，不执行点击。
- Tap Target 已支持通过 TargetResolver 解析图片、selector 和 text `targetRef`：匹配并返回坐标后才会串行点击并释放 pointer actions；低置信、未找到、不支持或基础设施错误都会进入暂停态，不盲点。
- 图片模板资产管理已落到 Runtime：目标库 JSON 只保存项目内相对 `imageRef`，模板 PNG 保存在本地 `targets/images`，解析时由 Runtime 临时读取并交给 TargetResolver。
- 目标库已具备图片目标“试找”工具：它只使用最近截图测试目标能否命中，不刷新截图、不启动驱动、不点击设备，并通过 Runtime 事件返回短中文结果。
- Flutter Workflow 节点库、顶部新增菜单、画布展示和 Inspector 已能识别 Wait For Target；缺失目标仍由 Runtime targetRef 校验拦截，不会把坏引用静默写入流程。
- Project DSL 已对 Tap、Visual Branch 和 Wait For Target 的 `targetRef`、置信阈值和轮询参数做基础形态校验；目标存在性仍由 Runtime 项目级 Target Library 校验负责。
- 已覆盖 Pyxelator fixture 匹配、Airtest fixture 断言、Python sidecar provider JSON 映射、Pyxelator / Airtest / builtin 后端分派、Python OCR text provider 映射、Source text 未命中后转 OCR、OCR 不可用时保留 Source 未命中、Python 内置后端真实脚本冒烟、Region target 解析和区域 Tap、Selector target source 解析和 selector Tap / Wait、Text target source 解析和 text Tap、Python 视觉开关持久化和设置抽屉回归、目标库图片试找工具、操作量上限、Visual Branch targetRef 通过与低置信暂停、Wait For Target 通过与低置信暂停、Tap Target 图片目标通过与低置信暂停、本地模板资产读写与路径安全、DSL targetRef / Wait For Target 参数校验、Workflow UI 插入回归。
- Airtest 报告心智已先落到 Runtime 本地报告模型；Monitor 报告面板和脱敏 JSON 复制导出已在 Batch 7 基础切片落地，完整 Evidence 保留策略升级继续留在 Batch 7 后续切片。

## 8. Batch 6: Workflow Canvas 4.0

### Goal

把 Workflow 从强大的编辑器升级为移动自动化编排中心。

### Deliverables

- Target 节点。
- Vision 节点。
- Inspector 节点辅助选择。
- 节点能力按平台提示。
- 子流程模板库。
- 节点级证据入口。
- 画布运行态增强。
- Visual / Source / Validate 完整一致。
- 复杂图编辑继续受 validator 保护。

### Acceptance

- 画布和源码映射同一 Project DSL。
- 节点能力缺失能提前提示。
- 运行中锁定编辑。
- iOS / Android 工作流共享 DSL。

### Current Implementation Slice

- Workflow 画布、源码和检查页已共用同一 Project DSL；源码草稿解析失败或 validator 失败时不会替换 Runtime 当前 workflow。
- Workflow 节点库、顶部新增菜单和边上插入菜单已覆盖 Tap、Wait、Swipe、Input、Loop、Snapshot、Condition、Visual Branch、Wait For Target、Catch 和 Sub Workflow，新增节点都通过 Runtime `updateWorkflow` 与 Project DSL validator 保存。
- Wait For Target 已作为 V4 视觉节点进入画布、源码、检查和 Inspector；缺失 `targetRef`、参数越界或目标不存在会进入同一诊断链路，不会静默写入坏流程。
- Snapshot 节点已在 Inspector 中提供本地证据开关；运行证据仍由 Runtime evidence store 写入，不由 UI 直接拼接路径。
- Workflow 画布已具备平移、缩放、适配、小地图、节点导航、框选、多选、对齐、均分、快捷键、撤销 / 重做、跨 workflow 系统剪贴板、端口连线、边删除、边重接、边上插入和 Catch 错误边编辑。
- 画布运行态已基于 Runtime execution focus 展示当前、完成和失败节点；Node Navigator 可快速定位当前节点、失败节点、选中节点和首个问题节点。
- Workflow 画布已接入最近一次运行的节点级留档入口：页面只通过 Runtime `readRunDetail` 读取轻量聚合，节点卡片显示“问题 / 截图 / 视觉 / 留档”短标；选中节点后 Inspector 展示“上次留档”卡，并可带着运行 ID 和节点 ID 深链打开记录详情，Monitor 会自动定位对应节点、展开截图证据或节点轨迹；Workflow 页面不直接扫描 evidence 文件、不读取截图内容、不保存本机路径。
- Device Inspector 已接入“建流程”辅助入口：用户检查当前界面后，可从第一个可读元素生成 selector 目标并插入 Tap `targetRef` 节点；保存只经过 Runtime `upsertTarget` 和 `updateWorkflow`，不会直接点击设备、启动任务、读取 XML 细节或调用 Legacy Node。
- Workflow 节点卡片已接入细粒度平台能力徽标：根据当前连接状态、iOS / Android 能力报告、目标库类型和视觉增强设置，显示“待连 / iOS 可用 / 安卓可用 / 缺截图 / 缺元素 / 缺文字 / 需目标 / 缺目标”等短提示；徽标只做 UI 派生，不写 Project DSL、不启动检查、不触发设备动作。
- 运行中或保存中会锁定画布、源码保存、模板导入、节点库插入和 Inspector 编辑，避免运行期改写 Project DSL。
- Visual / Source / Validate 诊断已复用同一 Source diagnostic 模型；Validate 诊断可切回画布选中节点，Source 诊断可定位字段，Inspector 节点问题只读展示。
- 当前 DSL、Runtime 和 Smoke Runner 已保持平台中立，iOS / Android 共享同一 Project DSL 节点语义；平台能力差异已能在 Device / Inspector / Report 与 Workflow 节点卡片中提前提示。
- 已覆盖 Workflow Source / Validate、画布连接、边编辑、视口、小地图、节点导航、节点能力徽标、节点库、模板、剪贴板、选区、Inspector 基础编辑、高级参数、子流程和运行锁定的 Flutter widget 回归测试。
- 独立 Target Library 一级模块仍是后续产品化增强，不影响 Batch 6 基础闭环。

## 9. Batch 7: Evidence, Report And Monitor 4.0

### Goal

吸收 Airtest 报告思路，形成本地可复盘系统。

### Deliverables

- 本地报告。
- 失败分类。
- 视觉证据链。
- 节点耗时。
- 平台差异分析。
- 日志摘要。
- 截图胶片。
- 报告导出。
- Evidence 保留策略升级。

### Acceptance

- 每次运行都能复盘。
- 每次视觉判断都有证据。
- 报告不泄露完整设备标识和本机路径。
- Android 冒烟运行也能生成报告。

### Current Implementation Slice

- Runtime 已具备 `RunLocalReport` 本地报告模型，可从 `RunDetail` 派生 overview、issue、timeline、visualChecks、screenshots、logSummary 和 platform。
- 本地报告只读取现有 evidence 真源，不新增第二套报告存储，不影响连接、执行和截图写入链路。
- 本地报告导出 JSON 会过滤不安全截图路径，并脱敏本机路径、设备号、长 session 和本机驱动地址。
- `LocalRunEvidenceStore` 已实现 `RunReportReader` 和 `RunReportExporter`，`StudioRuntimeController` 已提供 `readRunReport` 与 `exportRunReport` 入口，供后续 Monitor、导出和 AI 解释复用。
- Monitor / Execute 共用的 Run Detail 抽屉已接入 `readRunReport`，展示“本地报告”复盘面板，并提供“复制报告”和“存报告”两个脱敏 JSON 入口。
- “存报告”由 Runtime 写入当前运行目录内的 `exports/`，不让 UI 扫描 evidence 文件或拼接本机路径；该导出文件随单次运行目录一起进入本地证据生命周期。
- 报告已具备基础平台差异摘要：从 smoke / session / logs 事件提取 iOS / Android / unknown、设备短名、脱敏设备号、系统版本、连接方式、动作开关和日志数量；Monitor 本地报告面板会展示短中文平台提示，旧记录缺字段时降级为未知。
- Evidence 读取历史前会先应用同一套保留策略；App 重启后打开记录页或刷新历史也会滚动清理过期本地证据。
- 已覆盖本地报告聚合、视觉检查、截图胶片、日志摘要、平台摘要、Runtime 读取入口、Runtime 文件导出、Monitor 报告 UI、报告复制导出、报告保存、读前清理和隐私脱敏测试。
- 更深的跨平台指标型分析仍可后续增强，但 Batch 7 基础 Evidence / Report / Monitor 4.0 闭环已完成。

## 10. Batch 8: AI / MCP Core

### Goal

让产品具备 Cursor for Mobile Automation 的受控智能能力。

### Deliverables

- MCP-compatible Tool Registry。
- Tool Permission Gate。
- 流程草稿生成。
- 失败解释。
- 目标建议。
- Locator 建议。
- 模板修复建议。
- 用户确认机制。
- AI 行为日志。

### Acceptance

- AI 不直接执行危险动作。
- AI 不绕过 Runtime。
- AI 默认不上传截图。
- AI 输出可追踪。
- 没有 AI 环境时核心自动化仍可用。

### Current Implementation Slice

- Runtime 已具备 MCP-compatible `AiToolRegistry`，登记读屏摘要、流程草稿、失败解释、目标建议、Locator 建议、模板修复建议和运行流程交接工具。
- Runtime 已具备 `AiToolPermissionGate`：只读和草稿工具可自动执行，危险工具必须用户确认，未知或禁用工具直接阻断。
- `StudioRuntimeController.invokeAiTool` 是唯一 AI 工具入口；它只读取 Runtime 状态、本地报告和脱敏 Inspector 摘要，或生成草稿，不直接点击、不启动运行、不保存 workflow、不写目标库。
- Command Center 已接入“智能”抽屉，可调用读屏摘要、流程草稿、目标建议、Locator 建议、失败解释、模板修复建议和运行交接；抽屉只展示/复制脱敏结果和审计日志，不把草稿自动保存到 Project DSL，也不直接执行设备动作。
- Device Inspector 已接入“智能建议”入口，可在检查界面后调用目标建议和 Locator 建议；结果只在面板内展示草稿，不写 Target Library、不保存 workflow、不触发设备动作。
- Monitor Run Detail 已接入“智能解释”入口，可基于当前运行的本地报告调用失败解释；结果只读展示，不读取截图画面、不写报告文件、不触发重跑或修复。
- `runWorkflow` 工具即使用户确认，也只返回 `runtime.startRun` 交接结果，由 UI 和 Runtime 主按钮继续处理，AI 不拥有执行权。
- 读屏摘要只返回连接、运行、平台、能力、元素数量和是否有截图等摘要，不返回截图 base64。
- 失败解释和模板修复建议复用 Batch 7 本地报告模型，输出继续经过路径、设备号、session 和本机驱动地址脱敏。
- 目标建议和 Locator 建议只基于已脱敏元素树生成草稿，使用项目允许的 `label=`、`value=`、`type=` 短语法，不生成 XPath、CSS、脚本或任意表达式。
- Runtime snapshot 已包含最近 AI 行为审计日志；日志只记录工具、风险、状态、确认状态和短消息，最多保留最近 80 条，避免长期积累敏感上下文。
- 已覆盖权限门禁、危险工具确认、读屏摘要不泄露截图、目标 / Locator 草稿不持久化、失败解释脱敏、模板建议、流程草稿不写入当前 workflow、Command Center 智能抽屉、Device Inspector 智能建议、Run Detail 智能解释、脱敏结果复制和运行交接不直接启动的 Runtime / Flutter 测试。
- 当前 Batch 8 已具备 Core + Command Center + Device Inspector + Run Detail 可见入口闭环；后续继续增强时不得改变 AI 不直接执行动作的边界。

## 11. Stop Conditions

任一批次出现以下情况必须暂停：

- Flutter / Dart 主路径重新调用 Node。
- 第三方能力绕过 Runtime。
- iOS 基础流程退化。
- Android 首版冒烟无法闭环但仍继续堆 UI。
- 视觉低置信仍自动点击。
- 复制第三方源码但未登记许可证。
- 运行证据写入完整设备标识、本机路径或隐私信息。

## 12. Verification Baseline

每批至少考虑：

- Dart analyze。
- Dart / Flutter tests。
- V4 boundary check。
- Runtime fake driver tests。
- Workflow DSL validator tests。
- Vision fixture tests。
- `npm run v4:smoke-readiness` 生成本地脱敏 smoke readiness Markdown 留档和同名 JSON 摘要，并索引最近 full smoke JSON 的提交号、阻断项、步骤结果和 Android smoke 前置诊断；最近平台 smoke 也会读取 `smokeStart` 事件中的短提交号，旧提交或缺提交号的运行只作为历史线索，不计作当前提交完整通过；`--require-complete` 必须同时要求当前提交的 iOS smoke、Android smoke 和 full smoke 完整通过。
- `npm run v4:smoke-artifact-contract` 使用临时 fixture 验证 readiness / full smoke JSON 与 Markdown 留档结构，确保无真机环境下也能防止验收证据字段回归。
- `npm run v4:smoke-archive` 生成本地脱敏 smoke archive 清单，索引 readiness、full smoke、平台 run 和截图；最终验收可用 `npm run v4:smoke-archive:final` 要求当前提交的完整 full smoke、截图、iOS run 和 Android run 均已留档，且最近 iOS / Android 平台 run 也必须属于当前提交并完成截图、真实 Tap / Swipe / Input 动作和基础 workflow；旧提交或未完整的平台 run / full smoke 必须返回非 0。
- `npm run v4:acceptance-audit` 汇总 readiness、archive、completion audit 和 archive final 的本地审计报告，并在 JSON / Markdown 中嵌入短提交、分支、工作区干净度、上游同步状态、本机状态摘要、Batch 0-8 验收索引、Android 前置诊断、截图留档索引、终验门禁缺口、留档数量、现场补验清单和基于结构化证据生成的命令级下一步；截图索引只展示 archive 里的脱敏相对路径和大小，不读取图片内容、不展示绝对路径；Git 状态只保留短指纹、干净度和 ahead / behind 同步状态，不保存文件列表或远端 URL；终验门禁缺口必须同时吸收代码状态、archive warnings、readiness 当前本机设备状态和 readiness 最近平台 smoke 状态，转成“当前问题 / 通过标准 / 建议命令”的结构化表格，覆盖工作区未提交、提交未推送、当前设备未就绪、缺平台 run、已有平台 run 但最近未完整通过、已有 smoke 但不属于当前提交、截图缺失和 full smoke 未过；现场补验清单也必须读取 readiness 当前 iOS / Android 状态，把当前可用或未就绪写入每个平台的通过标准，避免现场用户只看到泛化命令；`npm run v4:acceptance-final` 只有在本地工作区干净、当前提交已与上游同步、当前提交的 iOS smoke、Android smoke、full smoke 和留档全部通过时才能返回 0；失败时也必须在终端输出同一组门禁缺口、现场补验清单和安全命令；最近 full smoke 若阻断在 iOS 隧道，双平台补验命令必须给出 `npm run v4:smoke:full:password-prompt`，避免普通 full smoke 再次卡密码；最终验收可用 `npm run v4:acceptance-final` 作为统一门禁。
- `npm run v4:smoke:full` 作为现场双平台完整冒烟入口，默认检查 Appium 平台 driver，自动准备本机驱动、iOS 隧道和 Android ADB 可见性，并在报告中写入“自动准备”区块和当前短提交号；Android 纳入 full smoke 且前置阻断时，同步生成 `ANDROID_SMOKE_PREFLIGHT` JSON / Markdown，供 readiness / acceptance 展示最新 Android 阻断。需要本机隧道密码时优先用 `npm run v4:ios-smoke:full:password-prompt` 或 `npm run v4:smoke:full:password-prompt`，密码通过终端隐藏输入进入 sudo stdin，不写日志、不进报告；`password-stdin` 入口仅作为自动化备用。单平台排障可用 `npm run v4:ios-smoke:full` 或 `npm run v4:android-smoke:full`，二者同样走 full smoke 编排器并自动准备，只跳过另一个平台，并把平台 smoke evidence 绑定到当前短提交号。
- Mac App Monitor 的 V4 验收卡只基于 Runtime 快照展示当前平台、iOS / Android 现场状态、本地运行数量、Batch 0-8 进度、代码干净度、远端同步状态、Android 留档数量、问题数量和下一步提示；Runtime 启动和 Monitor 刷新时会读取最新 `FINAL_ACCEPTANCE` 脱敏摘要，并对用户可见文本二次脱敏，UI 不直接扫描文件、不读取截图、不展示报告路径、底层命令输出、Git 文件列表或远端 URL。它提供 full smoke、双平台密码版、iOS 密码版、Android smoke、acceptance audit、acceptance final 和“现场路线”复制入口。现场路线优先使用 final acceptance 的 `fieldChecklist` 顺序，并且只复制项目内 V4 smoke / 终验白名单命令；旧报告缺少结构化清单时才回退到 nextSteps 推断。复制命令不直接启动设备动作，不读取本地 smoke 文件，不替代最终验收门禁。
- Device 本机指引提供“安卓准备”卡，面向现场用户提示开启 USB 调试、插线、点允许和复制 Android smoke 命令；该卡只复制命令，不启动 ADB、不启动驱动、不执行真机动作。Device 本机环境检查会同步显示 Android ADB 准备状态，ADB 不可用或手机未就绪只进入提醒态，不阻断 iOS 连接。
- `npm run v4:completion-audit` 作为最终完成门禁；最近 iOS smoke、Android smoke 和 full smoke 均属于当前提交，并完成截图、真实 Tap / Swipe / Input 动作和基础 workflow 成功留档前必须返回非 0。
- iOS 真机现场验证。
- Android 真机冒烟验证。
- 隐私扫描。
- Third-party notice 检查。
